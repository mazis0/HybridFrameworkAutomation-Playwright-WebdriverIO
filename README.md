# 🚀 HybridFrameworkAutomation — Playwright + WebdriverIO

A **Hybrid Test Automation Framework** that combines **Playwright** (Web automation) and **WebdriverIO** (Mobile Native automation).  
Designed to run **Web + Mobile tests together in a single end-to-end scenario**, using **Cucumber BDD** for readable and maintainable test steps.

---

## ✨ Features
- 🔹 Hybrid execution: Web (Playwright) + Mobile (WebdriverIO)
- 🔹 Cucumber BDD with readable `.feature` files
- 🔹 Page Object Model (POM) structure
- 🔹 Custom utilities & reusable functions
- 🔹 Tag-based test filtering
- 🔹 Execute tests on multiple environments

---

## 📦 Setup & Installation

### **1. Clone or Download the Project**
```bash
   git clone https://github.com/mazis0/HybridFrameworkAutomation-Playwright-WebdriverIO.git
```

### **2. Open in VS Code**

### **3. Install Dependencies**
```bash
   npm install
```

### **4. Open in VS Code**
```bash
   npx playwright install
```

## ▶️ Running Tests

### Run all tests:
```bash
   npm run test
```

### Run tests using tags::
```bash
   npm run test --tags="@test"
```

### Run all tests:
```bash
   npx playwright install
```

## 📁 Report

- Multiple Cucumber Report <br>
- Default Cucumber report <br>
- Logs <br>
- Screenshots of failure (Web and Mobile) <br>
- Trace of failure <br>
